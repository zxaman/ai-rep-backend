# Environment Configuration
# Copy this file to .env and configure as needed

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG_MODE=true

# File Upload Limits
MAX_FILE_SIZE_MB=10

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:4200,http://localhost:5200

# Phase 6: AI Feedback Configuration
# Choose ONE provider and configure its API key

# Option 1: OpenRouter (Recommended - supports multiple models)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free
# Other models: anthropic/claude-3.5-sonnet, google/gemini-pro-1.5, openai/gpt-4

# Option 2: Google Gemini
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
# Other models: gemini-1.5-pro, gemini-pro

# Active AI Provider (openrouter or gemini)
AI_PROVIDER=openrouter

# AI Generation Settings
AI_TIMEOUT=30
AI_MAX_RETRIES=2

# Notes:
# - If both keys are set, AI_PROVIDER determines which to use
# - OpenRouter offers free models and unified API for multiple providers
# - Gemini offers fast, cost-effective Google models
# - If no keys are configured, AI feedback will be disabled (app still works)
